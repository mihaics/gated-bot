__pycache__/
*.pyc
.venv/
config.yaml
.env
*.db
/tmp/
*.egg-info/
dist/
uv.lock
mcp/openbrain.json

# Claude Code settings are globally ignored (per-user state, local overrides)
.claude/
# ...except the persona's checked-in hook registration, which must ship
!persona/.claude/
!persona/.claude/**
